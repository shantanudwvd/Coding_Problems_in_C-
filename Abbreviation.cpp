//
// Created by aveorenzhio on 14/7/19.
//
#include <iostream>
#include <cmath>
#include <bits/stdc++.h>

using namespace std;

int main() {
    string str1, str2;
    cin >> str1;
    deque<char> orig;
    for (int i = 0; i < str1.size(); ++i) {
        orig.push_back(str1.at(i));
    }
    cin >> str2;
    deque<char> final;
    for (int j = 0; j < str2.size(); ++j) {
        final.push_back(str2.at(i));
    }
    deque<char>::iterator itr1, itr2;
}
